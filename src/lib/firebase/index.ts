/**
 * Firebase Client SDK Exports
 * Only client-safe modules (Auth, Client App, Client Config) are exported here.
 * For server-only utilities (Admin SDK, Storage Upload, Firestore Admin), import from '@/lib/firebase/server'.
 */
export * from './config';
export * from './client';
export * from './auth';
